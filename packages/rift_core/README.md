# RIFT core

This package contains RIFT's framework-agnostic domain models. It defines experiment and scenario specifications, deterministic fault rules, execution context, and structured run evidence.

It does not execute experiments, inject faults, run agents, evaluate invariants, or persist results. Those behaviors remain separate architectural boundaries.
